---
'@mastra/server': patch
'@mastra/client-js': patch
'@mastra/playground-ui': patch
---

Updated skill search result types and query parameters to use `skillName`/`skillNames` instead of `skillPath`/`skillPaths` for consistency with the name-based public API.
